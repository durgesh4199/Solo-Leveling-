import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.huntersguild.hunterprotocol",
  appName: "Hunter Protocol",
  webDir: "dist",
  backgroundColor: "#161826",
  android: {
    backgroundColor: "#161826"
  }
};

export default config;
