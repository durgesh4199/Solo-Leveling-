// No privileged APIs are exposed to the game — it only needs localStorage
// (available by default in the renderer) for saving progress. Kept as a
// placeholder so future native integrations (Steam overlay, file export,
// auto-update) have a documented, safe place to land.
